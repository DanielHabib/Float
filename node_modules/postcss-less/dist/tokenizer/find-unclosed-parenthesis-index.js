'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = findUnclosedParenthesisIndex;
function findUnclosedParenthesisIndex(state) {
    var openedParenthesesCount = 0;
    var i = state.tokens.length;

    while (i--) {
        var tokenName = state.tokens[i][0];

        if (tokenName === '(') {
            openedParenthesesCount++;
        } else if (tokenName === ')') {
            openedParenthesesCount--;
        }

        if (openedParenthesesCount > 0) {
            return i;
        }
    }

    return -1;
}
module.exports = exports['default'];